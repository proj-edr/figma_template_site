Place your visual baseline images in this folder:

- `Home-v1-desktop.png` for desktop comparison.
- `home-v1-mobile.png` for mobile comparison.

Then run:

- `npm run test:visual` to validate against those baselines.
- `npm run test:visual:update` to regenerate baselines from the current site.
